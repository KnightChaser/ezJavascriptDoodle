// logical operations
// or, and, not

const name = "knightchaser";
const age = 20;
const notHorny = false

if(name === "knightchaser" && age >= 20 && !notHorny) {
    console.log("You'are horny");
} else {
    console.log("You're not horny");
}